var computations = {
    animal_liking:function(data){
        var total;
        total = 0;
        total += data.horses;
        total += data.cats;
        total += data.dogs;
        return total;
    }
};
